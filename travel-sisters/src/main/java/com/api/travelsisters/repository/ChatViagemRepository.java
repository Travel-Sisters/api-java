package com.api.travelsisters.repository;

import com.api.travelsisters.model.ChatViagemModel;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ChatViagemRepository extends JpaRepository<ChatViagemModel, Integer> {
}
